# ========================================
# VidFlow Backend - Core Dependencies
# ========================================
# This file contains only essential dependencies for basic functionality.
# For optional features, see requirements-optional.txt
# For development tools, see requirements-dev.txt
# For AI features (Whisper subtitles), see requirements-ai.txt (Python 3.8-3.11)

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
websockets>=12.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Data Validation & Processing
pydantic>=2.0.0
python-dotenv>=1.0.0

# Async I/O & HTTP
aiofiles>=23.2.0
aiohttp>=3.9.0
httpx>=0.25.0
requests>=2.28.0  # Required by various dependencies

# Video Processing
yt-dlp>=2023.10.0

# System Utilities
psutil>=5.9.0

# Translation (for subtitle translation)
deep-translator>=1.11.0

# Browser Automation (for Cookie auto-extraction)
selenium>=4.15.0
webdriver-manager>=4.0.0  # 自动管理ChromeDriver下载和版本匹配
browser-cookie3>=0.19.1  # 从浏览器提取Cookie
pycryptodomex>=3.19.0  # Cookie 解密支持
pywin32>=306  # Windows Cookie 解密支持

# Build & Packaging
pyinstaller>=6.0.0
